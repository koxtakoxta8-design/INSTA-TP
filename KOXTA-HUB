--==================================================
-- KOXTA INSTA STEAL - BLACK EDITION
-- INTERFAZ + LOADING LIGADO A FOTO 2
--==================================================

local UIPlayers = game:GetService("Players")
local UITweenService = game:GetService("TweenService")
local UIUserInputService = game:GetService("UserInputService")

local UIPlayer = UIPlayers.LocalPlayer
local UIPlayerGui = UIPlayer:WaitForChild("PlayerGui")
local UILogo = "rbxassetid://103645600156879"

local UIGui = Instance.new("ScreenGui")
UIGui.Name = "KOXTA_INSTA_TP"
UIGui.ResetOnSpawn = false
UIGui.IgnoreGuiInset = true
UIGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
UIGui.Parent = UIPlayerGui

local function UITween(Object,Time,Properties,Style,Direction)
    local Info = TweenInfo.new(Time,Style or Enum.EasingStyle.Sine,Direction or Enum.EasingDirection.InOut)
    local T = UITweenService:Create(Object,Info,Properties)
    T:Play()
    return T
end

local UIPanel = Instance.new("Frame")
UIPanel.Name = "Panel"
UIPanel.Size = UDim2.fromOffset(255,190)
UIPanel.Position = UDim2.new(1,-270,0,78)
UIPanel.BackgroundColor3 = Color3.fromRGB(7,7,8)
UIPanel.BorderSizePixel = 0
UIPanel.ClipsDescendants = true
UIPanel.ZIndex = 20
UIPanel.Parent = UIGui

local UIPanelCorner = Instance.new("UICorner")
UIPanelCorner.CornerRadius = UDim.new(0,16)
UIPanelCorner.Parent = UIPanel

local UIPanelStroke = Instance.new("UIStroke")
UIPanelStroke.Thickness = 1.5
UIPanelStroke.Transparency = 0.25
UIPanelStroke.Color = Color3.fromRGB(255,255,255)
UIPanelStroke.Parent = UIPanel

local UIGradient = Instance.new("UIGradient")
UIGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,Color3.fromRGB(15,15,17)),
    ColorSequenceKeypoint.new(0.5,Color3.fromRGB(5,5,6)),
    ColorSequenceKeypoint.new(1,Color3.fromRGB(11,11,13))
})
UIGradient.Rotation = 45
UIGradient.Parent = UIPanel

for i=1,10 do
    local Line=Instance.new("Frame")
    Line.Size=UDim2.new(0,math.random(20,70),0,1)
    Line.Position=UDim2.new(0,math.random(5,245),0,math.random(5,180))
    Line.BackgroundColor3=Color3.fromRGB(255,255,255)
    Line.BackgroundTransparency=0.94
    Line.BorderSizePixel=0
    Line.ZIndex=21
    Line.Parent=UIPanel
end

for i=1,25 do
    local Particle=Instance.new("Frame")
    Particle.Size=UDim2.fromOffset(math.random(1,2),math.random(1,2))
    Particle.Position=UDim2.new(math.random(),0,math.random(),0)
    Particle.BackgroundColor3=Color3.fromRGB(255,255,255)
    Particle.BackgroundTransparency=math.random(75,95)/100
    Particle.BorderSizePixel=0
    Particle.ZIndex=22
    Particle.Parent=UIPanel
end

local Scanner=Instance.new("Frame")
Scanner.Size=UDim2.new(1,-20,0,1)
Scanner.Position=UDim2.new(0,10,0,45)
Scanner.BackgroundColor3=Color3.fromRGB(255,255,255)
Scanner.BackgroundTransparency=0.88
Scanner.BorderSizePixel=0
Scanner.ZIndex=23
Scanner.Parent=UIPanel

local UILogoImage=Instance.new("ImageLabel")
UILogoImage.Size=UDim2.fromOffset(58,58)
UILogoImage.Position=UDim2.new(0,15,0,12)
UILogoImage.BackgroundTransparency=1
UILogoImage.Image=UILogo
UILogoImage.ScaleType=Enum.ScaleType.Fit
UILogoImage.ZIndex=30
UILogoImage.Parent=UIPanel

local UITitle=Instance.new("TextLabel")
UITitle.Size=UDim2.new(1,-85,0,25)
UITitle.Position=UDim2.new(0,82,0,15)
UITitle.BackgroundTransparency=1
UITitle.Text="KOXTA INSTA TP"
UITitle.Font=Enum.Font.GothamBold
UITitle.TextSize=17
UITitle.TextColor3=Color3.fromRGB(220,220,225)
UITitle.TextXAlignment=Enum.TextXAlignment.Left
UITitle.ZIndex=40
UITitle.Parent=UIPanel

local UISubtitle=Instance.new("TextLabel")
UISubtitle.Size=UDim2.new(1,-90,0,18)
UISubtitle.Position=UDim2.new(0,83,0,38)
UISubtitle.BackgroundTransparency=1
UISubtitle.Text="[ BLACK EDITION ]"
UISubtitle.Font=Enum.Font.Gotham
UISubtitle.TextSize=10
UISubtitle.TextColor3=Color3.fromRGB(120,120,125)
UISubtitle.TextXAlignment=Enum.TextXAlignment.Left
UISubtitle.ZIndex=40
UISubtitle.Parent=UIPanel

local UIStatus=Instance.new("TextLabel")
UIStatus.Size=UDim2.new(1,-24,0,20)
UIStatus.Position=UDim2.new(0,12,0,78)
UIStatus.BackgroundTransparency=1
UIStatus.Text="●  SYSTEM READY"
UIStatus.Font=Enum.Font.GothamMedium
UIStatus.TextSize=11
UIStatus.TextColor3=Color3.fromRGB(205,205,210)
UIStatus.TextXAlignment=Enum.TextXAlignment.Left
UIStatus.ZIndex=40
UIStatus.Parent=UIPanel

local UIInstaButton=Instance.new("TextButton")
UIInstaButton.Size=UDim2.new(1,-24,0,38)
UIInstaButton.Position=UDim2.new(0,12,0,105)
UIInstaButton.BackgroundColor3=Color3.fromRGB(15,15,16)
UIInstaButton.BorderSizePixel=0
UIInstaButton.Text="INSTA TP   •   ON"
UIInstaButton.Font=Enum.Font.GothamBold
UIInstaButton.TextSize=12
UIInstaButton.TextColor3=Color3.fromRGB(205,205,210)
UIInstaButton.AutoButtonColor=false
UIInstaButton.ZIndex=45
UIInstaButton.Parent=UIPanel

local UIInstaCorner=Instance.new("UICorner")
UIInstaCorner.CornerRadius=UDim.new(0,10)
UIInstaCorner.Parent=UIInstaButton

local UIInstaStroke=Instance.new("UIStroke")
UIInstaStroke.Thickness=1
UIInstaStroke.Color=Color3.fromRGB(0,220,90)
UIInstaStroke.Transparency=0.25
UIInstaStroke.Parent=UIInstaButton

local UIReady=Instance.new("TextLabel")
UIReady.Size=UDim2.new(1,-24,0,18)
UIReady.Position=UDim2.new(0,12,0,150)
UIReady.BackgroundTransparency=1
UIReady.Text="INTERFACE READY"
UIReady.Font=Enum.Font.Gotham
UIReady.TextSize=9
UIReady.TextColor3=Color3.fromRGB(120,120,125)
UIReady.TextXAlignment=Enum.TextXAlignment.Left
UIReady.ZIndex=40
UIReady.Parent=UIPanel

local UIFloat=Instance.new("ImageButton")
UIFloat.Size=UDim2.fromOffset(54,54)
UIFloat.Position=UDim2.new(1,-68,0,16)
UIFloat.BackgroundColor3=Color3.fromRGB(0,0,0)
UIFloat.BorderSizePixel=0
UIFloat.Image=UILogo
UIFloat.ZIndex=200
UIFloat.Parent=UIGui

local UIFloatCorner=Instance.new("UICorner")
UIFloatCorner.CornerRadius=UDim.new(1,0)
UIFloatCorner.Parent=UIFloat

local UIFloatStroke=Instance.new("UIStroke")
UIFloatStroke.Thickness=2
UIFloatStroke.Color=Color3.fromRGB(255,255,255)
UIFloatStroke.Parent=UIFloat

--==================================================
-- LOADING: QUEDA VISIBLE HASTA FOTO 2
--==================================================

local UILoading=Instance.new("Frame")
UILoading.Name="LoadingScreen"
UILoading.Size=UDim2.fromScale(1,1)
UILoading.BackgroundColor3=Color3.fromRGB(0,0,0)
UILoading.BorderSizePixel=0
UILoading.Visible=false
UILoading.ZIndex=500
UILoading.Parent=UIGui

local UILoadingTitle=Instance.new("TextLabel")
UILoadingTitle.Size=UDim2.new(1,0,0,70)
UILoadingTitle.Position=UDim2.new(0,0,0.43,0)
UILoadingTitle.BackgroundTransparency=1
UILoadingTitle.Text="KOXTA INSTA STEAL"
UILoadingTitle.Font=Enum.Font.GothamBlack
UILoadingTitle.TextSize=32
UILoadingTitle.TextColor3=Color3.fromRGB(245,245,245)
UILoadingTitle.TextStrokeColor3=Color3.fromRGB(255,255,255)
UILoadingTitle.TextStrokeTransparency=0.75
UILoadingTitle.ZIndex=501
UILoadingTitle.Parent=UILoading

local UIShine=Instance.new("Frame")
UIShine.Size=UDim2.fromOffset(35,90)
UIShine.Position=UDim2.new(0,-70,0.43,-10)
UIShine.BackgroundColor3=Color3.fromRGB(255,255,255)
UIShine.BackgroundTransparency=0.88
UIShine.BorderSizePixel=0
UIShine.Rotation=15
UIShine.ZIndex=502
UIShine.Parent=UILoading

local UIShineGradient=Instance.new("UIGradient")
UIShineGradient.Transparency=NumberSequence.new({
    NumberSequenceKeypoint.new(0,1),
    NumberSequenceKeypoint.new(0.5,0),
    NumberSequenceKeypoint.new(1,1)
})
UIShineGradient.Parent=UIShine

local UILoadingText=Instance.new("TextLabel")
UILoadingText.Size=UDim2.new(1,0,0,25)
UILoadingText.Position=UDim2.new(0,0,0.55,0)
UILoadingText.BackgroundTransparency=1
UILoadingText.Text="LOADING"
UILoadingText.Font=Enum.Font.GothamMedium
UILoadingText.TextSize=11
UILoadingText.TextColor3=Color3.fromRGB(150,150,150)
UILoadingText.ZIndex=501
UILoadingText.Parent=UILoading

local UIBarBackground=Instance.new("Frame")
UIBarBackground.Size=UDim2.new(0.55,0,0,4)
UIBarBackground.Position=UDim2.new(0.225,0,0.62,0)
UIBarBackground.BackgroundColor3=Color3.fromRGB(30,30,30)
UIBarBackground.BorderSizePixel=0
UIBarBackground.ZIndex=501
UIBarBackground.Parent=UILoading

local UIBar=Instance.new("Frame")
UIBar.Size=UDim2.new(0,0,1,0)
UIBar.BackgroundColor3=Color3.fromRGB(255,255,255)
UIBar.BorderSizePixel=0
UIBar.ZIndex=502
UIBar.Parent=UIBarBackground

local UIPercent=Instance.new("TextLabel")
UIPercent.Size=UDim2.new(1,0,0,20)
UIPercent.Position=UDim2.new(0,0,0.64,0)
UIPercent.BackgroundTransparency=1
UIPercent.Text="0%"
UIPercent.Font=Enum.Font.Gotham
UIPercent.TextSize=9
UIPercent.TextColor3=Color3.fromRGB(100,100,100)
UIPercent.ZIndex=501
UIPercent.Parent=UILoading

local UIEnabled=true
local UISequenceRunning=false

local function UIShowLoading()
    UILoading.Visible=true
    UILoading.BackgroundTransparency=0
    UILoadingTitle.TextTransparency=1
    UILoadingText.TextTransparency=1
    UIBar.Size=UDim2.new(0,0,1,0)
    UIPercent.Text="0%"

    UITween(UILoadingTitle,0.45,{TextTransparency=0})
    UITween(UILoadingText,0.45,{TextTransparency=0})

    for i=0,100,2 do
        if not UILoading.Visible then return end
        UIBar.Size=UDim2.new(i/100,0,1,0)
        UIPercent.Text=tostring(i).."%"
        task.wait(0.015)
    end
    -- NO ocultar aquí.
end

local function UIHideLoading()
    if not UILoading.Visible then return end

    UITween(UILoading,0.35,{BackgroundTransparency=1},
        Enum.EasingStyle.Quad,Enum.EasingDirection.In)
    UITween(UILoadingTitle,0.25,{TextTransparency=1})
    UITween(UILoadingText,0.25,{TextTransparency=1})

    task.wait(0.4)

    UILoading.Visible=false
    UILoading.BackgroundTransparency=0
end

UIInstaButton.MouseButton1Click:Connect(function()
    UIEnabled=not UIEnabled
    if UIEnabled then
        UIInstaButton.Text="INSTA TP   •   ON"
        UIInstaButton.TextColor3=Color3.fromRGB(205,205,210)
        UIInstaStroke.Color=Color3.fromRGB(0,220,90)
        UIStatus.Text="●  SYSTEM READY"
    else
        UIInstaButton.Text="INSTA TP   •   OFF"
        UIInstaButton.TextColor3=Color3.fromRGB(230,70,70)
        UIInstaStroke.Color=Color3.fromRGB(230,70,70)
        UIStatus.Text="●  SYSTEM STANDBY"
    end
end)

UIFloat.MouseButton1Click:Connect(function()
    UIPanel.Visible=not UIPanel.Visible
end)

--==================================================
-- TRIGGER VISUAL DEL PRIMER PROMPT
--==================================================

local UIConnections={}

local function ConnectLoadingPrompt(Prompt)
    if not Prompt:IsA("ProximityPrompt") then return end
    if UIConnections[Prompt] then return end
    UIConnections[Prompt]=true

    Prompt.Triggered:Connect(function()
        if not UIEnabled then return end
        if UISequenceRunning then return end

        UISequenceRunning=true
        UIStatus.Text="●  SYSTEM ACTIVE"
        task.spawn(UIShowLoading)
    end)
end

for _,Object in ipairs(workspace:GetDescendants()) do
    if Object:IsA("ProximityPrompt") then
        ConnectLoadingPrompt(Object)
    end
end

workspace.DescendantAdded:Connect(function(Object)
    if Object:IsA("ProximityPrompt") then
        ConnectLoadingPrompt(Object)
    end
end)

--==================================================
-- DETECTOR REAL DE FOTO 2
--==================================================

local FinalPhoto2=Vector3.new(544.83,70.28,-363.31)

task.spawn(function()
    while UIGui.Parent do
        if UISequenceRunning and UILoading.Visible then

            local Character=UIPlayer.Character
            local Root=Character and Character:FindFirstChild("HumanoidRootPart")

            if Root then
                local Distance=(Root.Position-FinalPhoto2).Magnitude

                if Distance <= 2 then
                    task.wait()

                    local Character2=UIPlayer.Character
                    local Root2=Character2 and Character2:FindFirstChild("HumanoidRootPart")

                    if Root2 and (Root2.Position-FinalPhoto2).Magnitude <= 2 then
                        UISequenceRunning=false
                        UIStatus.Text="●  SYSTEM READY"
                        UIHideLoading()
                    end
                end
            end
        end

        task.wait(0.03)
    end
end)



--==================================================
-- SECUENCIA 20 CICLOS
-- FOTO 1 -> AGARRAR -> FOTO 2
--
-- PROMPT MANUAL
-- -> 20 CICLOS
-- -> RESPAWN ESTABLE
-- -> DESLIZAMIENTO FOTO 1
-- -> 65% -> TP FOTO 1
-- -> BUSCAR PROMPT CERCANO
-- -> AGARRAR RAPIDO
-- -> CONFIRMAR
-- -> TP FOTO 2
--==================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local Player = Players.LocalPlayer
local Busy = false
local Connections = {}

--==================================================
-- PUNTOS
--==================================================

local RespawnPoint =
    Vector3.new(532.64, 70.28, -366.98)

-- FOTO 1 = DONDE ESTA EL HUEVO
local Photo1 =
    Vector3.new(566.45, 70.28, -371.29)

-- FOTO 2 = DESTINO FINAL
local Photo2 =
    Vector3.new(544.83, 70.28, -363.31)

--==================================================
-- CAMINADORAS
--==================================================

local Caminadoras = {
    Vector3.new(528.79, 70.28, -445.45),
    Vector3.new(500.32, 71.62, -445.89),
    Vector3.new(503.88, 70.28, -374.50),
    Vector3.new(501.67, 71.62, -324.69),
    Vector3.new(500.17, 71.62, -282.29)
}

--==================================================
-- CAMINADORA MAS CERCANA
--==================================================

local NearestPoint
local NearestDistance = math.huge

for _, Position in ipairs(Caminadoras) do

    local Distance =
        (RespawnPoint - Position).Magnitude

    if Distance < NearestDistance then
        NearestDistance = Distance
        NearestPoint = Position
    end
end

--==================================================
-- TP
--==================================================

local function TP(Character, Position)

    if not Character or not Character.Parent then
        return false
    end

    Character:PivotTo(
        CFrame.new(Position)
    )

    return true
end

--==================================================
-- DESLIZAMIENTO A FOTO 1
-- 1000 DE VELOCIDAD
-- 65% -> TP INMEDIATO
--==================================================

local function MoveToPhoto1(Character)

    if not Character or not Character.Parent then
        return false
    end

    local Root =
        Character:FindFirstChild("HumanoidRootPart")

    if not Root then
        return false
    end

    local Speed = 1000
    local ArrivalDistance = 0.5

    local StartPosition = Root.Position

    local TotalDistance =
        (Photo1 - StartPosition).Magnitude

    if TotalDistance <= ArrivalDistance then
        Character:PivotTo(CFrame.new(Photo1))
        return true
    end

    print("DESLIZAMIENTO -> FOTO 1")

    while Character.Parent and Root.Parent do

        local CurrentPosition = Root.Position

        local Distance =
            (Photo1 - CurrentPosition).Magnitude

        if Distance <= ArrivalDistance then

            Character:PivotTo(
                CFrame.new(Photo1)
            )

            return true
        end

        --==========================================
        -- CALCULAR PORCENTAJE
        --==========================================

        local DistanceTravelled =
            TotalDistance - Distance

        local Progress =
            DistanceTravelled / TotalDistance

        --==========================================
        -- 65% -> TP FOTO 1
        --==========================================

        if Progress >= 0.65 then

            print("65% -> TP FOTO 1")

            Character:PivotTo(
                CFrame.new(Photo1)
            )

            RunService.Heartbeat:Wait()

            -- Reforzar posición
            if Root.Parent then
                Character:PivotTo(
                    CFrame.new(Photo1)
                )
            end

            print("FOTO 1 CONFIRMADA")

            return true
        end

        --==========================================
        -- DESLIZAMIENTO
        --==========================================

        local Direction =
            (Photo1 - CurrentPosition).Unit

        local Delta =
            RunService.Heartbeat:Wait()

        local Step =
            math.min(
                Speed * Delta,
                Distance
            )

        Root.CFrame =
            CFrame.new(
                CurrentPosition +
                Direction * Step
            )
    end

    return false
end

--==================================================
-- POSICION DE CUALQUIER PROMPT
--==================================================

local function GetPromptPosition(Prompt)

    if not Prompt or not Prompt.Parent then
        return nil
    end

    local Parent = Prompt.Parent

    if Parent:IsA("BasePart") then
        return Parent.Position
    end

    if Parent:IsA("Attachment") then
        return Parent.WorldPosition
    end

    local Part =
        Parent:FindFirstChildWhichIsA(
            "BasePart",
            true
        )

    if Part then
        return Part.Position
    end

    return nil
end

--==================================================
-- BUSCAR EL PROXIMITYPROMPT MAS CERCANO
--
-- NO FILTRA POR NOMBRE.
-- PUEDE SER HUEVO, CAJA, OBJETO, ETC.
--==================================================

local function FindNearestPrompt(Root)

    if not Root or not Root.Parent then
        return nil
    end

    local BestPrompt = nil
    local BestDistance = math.huge

    for _, Object in ipairs(
        workspace:GetDescendants()
    ) do

        if Object:IsA("ProximityPrompt")
            and Object.Enabled then

            local Position =
                GetPromptPosition(Object)

            if Position then

                local Distance =
                    (Root.Position - Position).Magnitude

                if Distance < BestDistance then

                    BestDistance = Distance
                    BestPrompt = Object

                end
            end
        end
    end

    return BestPrompt
end

--==================================================
-- AUTO GRAB RAPIDO
--
-- BUSCA CUALQUIER PROMPT CERCANO
-- Y REINTENTA DURANTE UN PEQUEÑO MARGEN.
--==================================================

local function AutoGrab()

    local Character =
        Player.Character

    if not Character or not Character.Parent then
        return false
    end

    local Root =
        Character:FindFirstChild(
            "HumanoidRootPart"
        )

    if not Root then
        return false
    end

    -- Dar un instante para que el objeto
    -- quede completamente cargado después del TP.
    task.wait(0.15)

    local StartTime = os.clock()
    local Timeout = 1.5

    while os.clock() - StartTime < Timeout do

        Character = Player.Character

        if not Character or not Character.Parent then
            return false
        end

        Root =
            Character:FindFirstChild(
                "HumanoidRootPart"
            )

        if not Root then
            return false
        end

        local Prompt =
            FindNearestPrompt(Root)

        if Prompt then

            print(
                "PROMPT ENCONTRADO: "
                .. Prompt:GetFullName()
            )

            pcall(function()
                Prompt.HoldDuration = 0
                Prompt.RequiresLineOfSight = false
            end)

            --======================================
            -- CONFIRMACION
            --======================================

            local Confirmed = false

            local Connection

            Connection =
                Prompt.Triggered:Connect(
                    function()
                        Confirmed = true
                    end
                )

            --======================================
            -- ACTIVACION RAPIDA
            --======================================

            pcall(function()
                Prompt:InputHoldBegin()
                Prompt:InputHoldEnd()
            end)

            --======================================
            -- ESPERAR CONFIRMACION
            --======================================

            local ConfirmStart = os.clock()

            while
                not Confirmed
                and
                os.clock() - ConfirmStart < 0.35
            do

                RunService.Heartbeat:Wait()

                if not Prompt.Parent then
                    Confirmed = true
                    break
                end

                if not Prompt.Enabled then
                    Confirmed = true
                    break
                end
            end

            if Connection then
                Connection:Disconnect()
            end

            --======================================
            -- CONFIRMADO
            --======================================

            if Confirmed then

                print("OBJETO AGARRADO")

                return true
            end

            --======================================
            -- REINTENTO RAPIDO
            --======================================

            if Prompt.Parent and Prompt.Enabled then

                pcall(function()
                    Prompt:InputHoldBegin()
                    Prompt:InputHoldEnd()
                end)

                RunService.Heartbeat:Wait()

                if not Prompt.Parent
                    or not Prompt.Enabled then

                    return true
                end
            end
        end

        RunService.Heartbeat:Wait()
    end

    print("NO SE CONFIRMO EL GRAB")

    return false
end

--==================================================
-- RESPAWN ESTABLE
--==================================================

local function WaitForStableRespawn(OldCharacter)

    print("ESPERANDO RESPAWN...")

    local Character

    repeat
        task.wait()
        Character = Player.Character
    until Character
        and Character ~= OldCharacter
        and Character.Parent

    local Humanoid =
        Character:WaitForChild("Humanoid")

    local Root =
        Character:WaitForChild(
            "HumanoidRootPart"
        )

    while Humanoid.Health <= 0 do
        task.wait()
    end

    local LastPosition = Root.Position
    local StableTime = 0

    while StableTime < 0.4 do

        task.wait(0.05)

        if not Character.Parent then
            return WaitForStableRespawn(
                OldCharacter
            )
        end

        if not Root.Parent then

            Root =
                Character:WaitForChild(
                    "HumanoidRootPart"
                )

            LastPosition = Root.Position
            StableTime = 0

            continue
        end

        local CurrentPosition = Root.Position

        local Movement =
            (CurrentPosition - LastPosition).Magnitude

        if Movement < 0.05 then
            StableTime += 0.05
        else
            StableTime = 0
        end

        LastPosition = CurrentPosition
    end

    print("RESPAWN ESTABLE")

    return Character
end

--==================================================
-- SECUENCIA PRINCIPAL
--==================================================

local function StartSequence()

    if Busy then
        return
    end

    Busy = true

    local Character =
        Player.Character

    if not Character then
        Busy = false
        return
    end

    print("INICIANDO SECUENCIA")

    --================================================
    -- 20 CICLOS
    -- MISMA MECANICA ORIGINAL
    --================================================

    for Cycle = 1, 20 do

        if not Character
            or not Character.Parent then

            Busy = false
            return
        end

        TP(
            Character,
            RespawnPoint
        )

        RunService.Heartbeat:Wait()

        if NearestPoint then

            TP(
                Character,
                NearestPoint
            )
        end

        RunService.Heartbeat:Wait()

        print(
            "CICLO "
            .. Cycle
            .. "/20"
        )
    end

    print("20 CICLOS TERMINADOS")

    --================================================
    -- RESPAWN
    --================================================

    local OldCharacter = Character

    local Humanoid =
        OldCharacter:FindFirstChildOfClass(
            "Humanoid"
        )

    if not Humanoid then
        Busy = false
        return
    end

    Humanoid.Health = 0

    Character =
        WaitForStableRespawn(
            OldCharacter
        )

    if not Character then
        Busy = false
        return
    end

    --================================================
    -- FOTO 1
    -- 65% + TP
    --================================================

    if not MoveToPhoto1(Character) then

        print(
            "NO SE PUDO LLEGAR A FOTO 1"
        )

        Busy = false
        return
    end

    --================================================
    -- ASEGURAR FOTO 1
    --================================================

    Character =
        Player.Character

    if not Character
        or not Character.Parent then

        Busy = false
        return
    end

    TP(
        Character,
        Photo1
    )

    RunService.Heartbeat:Wait()

    --================================================
    -- AGARRAR OBJETO
    --================================================

    local Grabbed =
        AutoGrab()

    if not Grabbed then

        print(
            "GRAB NO CONFIRMADO"
        )

        Busy = false
        return
    end

    --================================================
    -- GRAB CONFIRMADO
    -- TP INMEDIATO FOTO 2
    --================================================

    Character =
        Player.Character

    if not Character
        or not Character.Parent then

        Busy = false
        return
    end

    print(
        "GRAB CONFIRMADO -> FOTO 2"
    )

    TP(
        Character,
        Photo2
    )

    print(
        "FOTO 2 CONFIRMADA"
    )

    print(
        "SECUENCIA TERMINADA"
    )

    Busy = false
end

--==================================================
-- PROMPT MANUAL
--==================================================

local function ConnectPrompt(Prompt)

    if not Prompt:IsA(
        "ProximityPrompt"
    ) then
        return
    end

    if Connections[Prompt] then
        return
    end

    Connections[Prompt] =
        Prompt.Triggered:Connect(
            function()

                if Busy then
                    return
                end

                print(
                    "PROMPT MANUAL ACTIVADO"
                )

                task.spawn(
                    StartSequence
                )
            end
        )
end

--==================================================
-- PROMPTS EXISTENTES
--==================================================

for _, Object in ipairs(
    workspace:GetDescendants()
) do

    if Object:IsA(
        "ProximityPrompt"
    ) then

        ConnectPrompt(Object)
    end
end

--==================================================
-- PROMPTS NUEVOS
--==================================================

workspace.DescendantAdded:Connect(
    function(Object)

        if Object:IsA(
            "ProximityPrompt"
        ) then

            ConnectPrompt(Object)
        end
    end
)

print("======================================")
print(" 20 CICLOS + FOTO 1 + AUTO GRAB")
print(" FOTO 1: 566.45, 70.28, -371.29")
print(" FOTO 2: 544.83, 70.28, -363.31")
print(" 65% -> TP FOTO 1")
print(" PROMPT MAS CERCANO")
print(" GRAB RAPIDO -> FOTO 2")
print("======================================")
